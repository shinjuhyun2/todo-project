# My focusing time  
### Analysis document

---

| Student No | Name | E-mail |
|------------|------|--------|
| 22411976 | 신주현 | s5264075@gmail.com |

---

## [ Revision history ]

| Revision date | Version # | Description | Author |
|---------------|----------|-------------|--------|
|  05/31/2026   |  1.00    | First Writing | 신주현 |

---

# = Contents =

1. Introduction
2. Class diagram 
3. Sequence diagram
4. State machine diagram  
5. Implementation requirements
6. Glossary
7. References

---

# 1. Introduction

사람들은 오래전부터 해야 할 일이나 중요한 일정들을 잊지 않기 위해 메모를 하거나 달력에 기록하는 방식으로 자신의 일정을 관리해 왔다. 현대에 들어서는 스마트폰과 다양한 애플리케이션의 발전으로 인해 디지털 방식의 일정 관리가 보편화되었으며, 많은 사람들이 To-Do List 애플리케이션을 통해 자신의 작업과 일정을 관리하고 있다.

하지만 기존의 일정 관리 애플리케이션들은 지나치게 많은 기능과 복잡한 인터페이스를 제공하는 경우가 많아, 단순히 해야 할 일을 빠르고 편리하게 기록하고 싶은 사용자들에게는 오히려 불편함을 줄 수 있다. 또한 중요한 작업을 우선적으로 관리하거나 원하는 작업을 빠르게 찾는 기능이 직관적이지 않아 사용성이 떨어지는 경우도 존재한다.

그래서 사용자가 보다 쉽고 효율적으로 자신의 작업을 관리할 수 있도록 To-Do List 프로그램 “My Focusing Time”을 개발하게 되었다. 본 시스템은 작업의 추가, 수정, 삭제 및 조회 기능을 중심으로 구성되며, 우선순위 설정과 검색 기능을 통해 사용자가 자신의 작업을 체계적으로 관리할 수 있도록 설계하였다.

특히 모바일 환경에서도 간단하고 직관적으로 사용할 수 있는 인터페이스를 목표로 하였으며, 복잡한 기능보다는 실제 사용자들이 자주 사용하는 핵심 기능들을 중심으로 시스템을 구성하였다. 이를 통해 학생이나 직장인 등 다양한 사용자들이 자신의 일정과 해야 할 일을 보다 효율적으로 관리할 수 있도록 하는 것을 목표로 한다.

본 문서에서는 “My Focusing Time” 시스템의 설계 내용을 다루고 있으며, 실제 구현에 필요한 클래스 구조와 객체 간의 관계, 주요 기능의 동작 과정 등을 구체적으로 정의하고 설계하는 내용을 포함한다.

---

# 2. Class diagram

[!diagram](./Class_diagram3.png)
[!diagram](./Class_diagram3_capture.png)

아래의 표는 위의 Class Diagram에서 표현한 Class들의 대한 설명이다.

| Class Name | Explanation | 
|------------|------|
| Task | 사용자가 등록할 작업(Task) 정보를 저장하고 관리하는 클래스이다.<br><br>작업의 제목(title), 마감일(dueDate), 우선순위(priority), 완료 여부(completed) 등의 데이터를 관리한다. |
| TaskManager | 시스템에 등록된 여러 개의 작업(Task) 객체를 관리하는 클래스이다. 내부적으로 taskList에 작업 목록을 저장하며 작업 추가, 삭제, 수정, 조회 및 정렬 기능을 수행한다.<br><br>- AddTask(task : Task) : 사용자가 입력한 정보를 바탕으로 작업을 추가하는 메서드이다.<br>- DeleteTask(task : Task) : 선택한 작업 객체를 taskList에서 제거하는 메소드이다.<br>- UpdateTask(task : Task) : 기존 작업의 정보를 수정하는 메소드이다.<br>- ViewTasks() : 현재 저장되어 있는 전체 작업 목록을 조회하는 메소드이다.<br>- searchTasks(keyword : String) : 사용자가 입력한 키워드를 기준으로 작업을 검색하는 메소드이다.<br>- sortTasks() : 작업 목록을 정렬하는 메소드이다.<br>- saveTask(task : Task) : 수정된 작업 정보를 저장하는 메소드이다. |
| Validator | 사용자가 입력한 작업 정보의 유효성을 검사하는 클래스이다. 작업 추가 또는 수정 과정에서 잘못된 데이터가 저장되지 않도록 입력값을 검증한다.<br><br>- ValidateTask(task : Task) : 작업 정보의 유효성을 검사하고 결과를 Boolean 값으로 반환하는 메소드이다. |

---

# 3. Sequence diagram

### 3.1 AddTask Sequence Diagram 

[!diagram](./AddTask.png)
[!diagram](./AddTask_capture.png)


시스템에서 작업 추가 기능을 시작하면 사용자가 작업 정보를 입력하고 AddTask를 요청한다. TaskManager는 입력된 작업 정보를 Validator로 전달하여 ValidateTask를 수행한다. Validator는 입력값의 유효성을 검사한 뒤 결과를 반환한다.

검사 결과가 성공(True)인 경우 TaskManager는 새로운 Task 객체를 생성(create)하고 사용자에게 작업 추가 완료 메시지를 출력한다. 이후 갱신된 작업 목록을 조회(ViewTasks)하여 사용자에게 표시한다.

반대로 유효성 검사 결과가 실패(False)인 경우 시스템은 사용자에게 ERROR 메시지를 출력하여 잘못된 입력임을 알린다.


### 3.2 DeleteTask Sequence Diagram 

[!diagram](./DeleteTask.png)
[!diagram](./DeleteTask_capture.png)

시스템에서 작업 삭제 기능을 시작하면 사용자가 삭제할 작업을 선택하고 DeleteTask를 요청한다.

사용자가 삭제를 취소한 경우(User cancels DeleteTask) TaskManager는 현재 작업 목록(ViewTasks)을 다시 조회하여 사용자에게 표시하고 동작을 종료한다.

삭제를 진행하는 경우 TaskManager는 선택된 Task 객체를 destroy하여 삭제를 수행한다. 이후 사용자에게 삭제 완료 메시지를 출력하고, 갱신된 작업 목록(ViewTasks)을 다시 조회하여 표시한다.


### 3.3 ModifyTask Sequence Diagram

[!diagram](./ModifyTask.png)
[!diagram](./ModifyTask_capture.png)

시스템에서 작업 정렬 기능을 시작하면 사용자가 SortTasks를 요청한다. TaskManager는 사용자에게 정렬 기준 선택 화면(Display sort selection screen)을 제공한다.

사용자가 정렬 작업을 취소한 경우(User cancels SortTasks) TaskManager는 현재 작업 목록(ViewTasks)을 다시 조회하여 사용자에게 표시하고 동작을 종료한다.

정렬할 작업이 존재하지 않는 경우(No task to sort) 시스템은 사용자에게 ERROR 메시지를 출력하여 정렬 가능한 작업이 없음을 알린다.

정상적으로 정렬이 수행되는 경우 TaskManager는 선택된 기준에 따라 작업 목록을 정렬한 뒤 사용자에게 정렬된 작업 목록 화면(Display sorted List Screen)을 출력한다.


### 3.4 ViewTask Sequence Diagram

[!diagram](./ViewTask.png)
[!diagram](./ViewTask_capture.png)

시스템에서 작업 조회 기능을 시작하면 사용자가 ViewTasks를 요청한다. TaskManager는 저장된 작업 목록을 조회한 뒤 결과를 반환한다.

조회 결과가 비어 있는 경우(result is empty) 시스템은 사용자에게 빈 화면(Display Empty screen)을 출력하여 등록된 작업이 없음을 알린다. 반대로 작업이 존재하는 경우 시스템은 작업 목록 화면(Display tasklist screen)을 출력한다.

이후 사용자가 검색 기능을 수행하는 경우(SearchTasks) TaskManager는 검색 결과를 조회한 뒤 반환한다.

검색 결과가 존재하지 않는 경우(result is empty) 시스템은 사용자에게 “No result” 메시지를 출력한다. 반대로 검색 결과가 존재하는 경우 시스템은 검색 조건에 맞는 작업 목록(View Tasks based on Search)을 사용자에게 표시한다.


### 3.5 SortTask Sequence Diagram

[!diagram](./SortTask.png)
[!diagram](./SortTask_capture.png)


시스템에서 작업 정렬 기능을 시작하면 사용자가 SortTasks를 요청한다. TaskManager는 사용자에게 정렬 기준 선택 화면(Display sort selection screen)을 제공한다.

사용자가 정렬 작업을 취소한 경우(User cancels SortTasks) TaskManager는 현재 작업 목록(ViewTasks)을 다시 조회하여 사용자에게 표시하고 동작을 종료한다.

정렬할 작업이 존재하지 않는 경우(No task to sort) 시스템은 사용자에게 ERROR 메시지를 출력하여 정렬 가능한 작업이 없음을 알린다.

정상적으로 정렬이 수행되는 경우 TaskManager는 선택된 기준에 따라 작업 목록을 정렬한 뒤 사용자에게 정렬된 작업 목록 화면(Display sorted List Screen)을 출력한다.




